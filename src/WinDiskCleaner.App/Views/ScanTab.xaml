<UserControl x:Class="WinDiskCleaner.App.Views.ScanTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="扫描 / AI 清理建议" FontSize="28" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,24,0" />
            <TextBox x:Name="DrivePathTextBox" Width="260" Height="36" Text="/home/ubuntu/WinDiskCleaner" Margin="0,0,8,0" />
            <Button Content="扫描" Width="88" Height="36" Click="ScanButton_Click" Margin="0,0,8,0" />
            <Button Content="AI 分析" Width="100" Height="36" Click="AnalyzeButton_Click" />
            <TextBlock x:Name="StatusTextBlock" Text="等待扫描报告" Margin="16,8,0,0" Foreground="#6B7280" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock x:Name="ScanSummaryTextBlock" Text="尚未扫描" Foreground="#374151" />
        </StackPanel>

        <ListView x:Name="SuggestionListView" Grid.Row="2" ItemsSource="{Binding Suggestions}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Selected, Mode=TwoWay}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="路径" DisplayMemberBinding="{Binding Path}" Width="360" />
                    <GridViewColumn Header="原因" DisplayMemberBinding="{Binding Reason}" Width="220" />
                    <GridViewColumn Header="风险" DisplayMemberBinding="{Binding Risk}" Width="80" />
                    <GridViewColumn Header="大小" DisplayMemberBinding="{Binding SizeBytes}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="执行清理" Width="120" Height="36" Click="ExecuteCleanButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
