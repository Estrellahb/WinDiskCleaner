<UserControl x:Class="WinDiskCleaner.App.Views.CleanSuggestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:WinDiskCleaner.Core.Models;assembly=WinDiskCleaner.Core">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type models:ScanTreeViewNode}" ItemsSource="{Binding Children}">
            <Grid ToolTip="{Binding DetailText}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Ellipse Width="8" Height="8" Fill="{Binding RiskColor}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="  ·  " Foreground="#AAA"/>
                    <TextBlock Text="{Binding SizeText}" Foreground="#555"/>
                    <TextBlock Text="  ·  " Foreground="#AAA"/>
                    <TextBlock Text="{Binding FileCountText}" Foreground="#777"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding PercentText}" Foreground="#777" Margin="12,0,0,0"/>
                <TextBlock Grid.Column="3" Text="{Binding RiskLevel}" Foreground="{Binding RiskColor}" Margin="12,0,0,0"/>
            </Grid>
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部扫描区 -->
        <Border Grid.Row="0" Padding="12" Margin="0,0,0,10" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Background="#FAFAFA">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="选择盘符：" VerticalAlignment="Center" FontSize="14"/>
                    <ComboBox x:Name="DriveCombo" Width="120" Margin="10,0"/>
                    <Button x:Name="ScanBtn" Content="开始扫描" Width="100" Height="30"
                            Background="#0078D4" Foreground="White" Click="ScanBtn_Click"/>
                    <Button x:Name="CancelBtn" Content="取消" Width="80" Height="30" Margin="10,0" IsEnabled="False" Click="CancelBtn_Click"/>
                    <ProgressBar x:Name="ScanProgress" Width="220" Height="20" Margin="10,0"/>
                </StackPanel>
                <TextBlock x:Name="ScanStatusText" DockPanel.Dock="Right" Text="等待扫描" Foreground="#666" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- 磁盘容量卡片 -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Padding="12" Margin="0,0,8,0" Background="#EEF6FF" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="总容量" Foreground="#555"/>
                    <TextBlock Text="{Binding TotalSizeText}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Padding="12" Margin="0,0,8,0" Background="#FFF7E6" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="已用" Foreground="#555"/>
                    <TextBlock Text="{Binding UsedSizeText}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Padding="12" Margin="0,0,8,0" Background="#ECFDF3" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="剩余" Foreground="#555"/>
                    <TextBlock Text="{Binding FreeSizeText}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Padding="12" Background="{Binding SpaceStatusBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="空间状态" Foreground="#555"/>
                    <TextBlock Text="{Binding SpaceStatusText}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 完整目录树 -->
            <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <TextBlock Text="完整目录树" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="默认展开 2 层，继续展开节点可查看更深层级；不再把 Top10 之外合并为其他。" Foreground="#666" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TreeView x:Name="DirectoryTreeView"
                              ItemsSource="{Binding DirectoryTree}"
                              SelectedItemChanged="DirectoryTree_SelectedItemChanged"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                </DockPanel>
            </Border>

            <!-- AI 建议和节点详情 -->
            <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <TextBlock Text="节点详情" FontSize="16" FontWeight="Bold"/>
                        <Border Padding="10" Margin="0,6,0,12" BorderBrush="#EEE" BorderThickness="1" CornerRadius="4" Background="#FAFAFA">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedNodeName}" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding SelectedNodePath}" Foreground="#666" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                <UniformGrid Columns="4" Margin="0,8,0,0">
                                    <StackPanel>
                                        <TextBlock Text="类型" Foreground="#777" FontSize="12"/>
                                        <TextBlock Text="{Binding SelectedNodeKindText}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="大小" Foreground="#777" FontSize="12"/>
                                        <TextBlock Text="{Binding SelectedNodeSizeText}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="文件数" Foreground="#777" FontSize="12"/>
                                        <TextBlock Text="{Binding SelectedNodeFileCountText}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="风险" Foreground="#777" FontSize="12"/>
                                        <TextBlock Text="{Binding SelectedNodeRiskText}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </UniformGrid>
                            </StackPanel>
                        </Border>
                        <TextBlock Text="AI 建议列表" FontSize="16" FontWeight="Bold"/>
                        <TextBlock x:Name="AiStatusText" Text="AI 建议：等待扫描" Foreground="#666" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding AiSuggestions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="10" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Row="0" Grid.Column="0" IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,8,0"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path}" FontWeight="Bold" TextWrapping="Wrap"/>
                                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding ConfidenceText}" Foreground="#666" Margin="8,0,0,0"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Reason}" Foreground="#666" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding SizeText}" Foreground="#999" FontSize="12" Margin="0,4,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="导出报告" Width="100" Height="30" Margin="0,0,10,0" Click="ExportBtn_Click"/>
            <Button Content="执行清理" Width="100" Height="30" Background="#107C10" Foreground="White" Click="ExecuteCleanBtn_Click"/>
            <TextBlock Text="{Binding CleanResultText}" Foreground="#666" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
